# 외부 API 키 (실제 키는 .env 에만, 절대 커밋 금지)
# 주의: 키 이름 뒤에 공백을 두지 말 것 (KEY=value 형식). 공백이 있으면 로딩이 실패할 수 있음.
DART_API_KEY=
ECOS_API_KEY=

# 데이터베이스 (개발: SQLite 기본값 사용 시 비워둬도 됨)
# DATABASE_URL=sqlite:///./data/cache/valuation.db
# 배포(PostgreSQL) 예: postgresql+psycopg://user:pass@host:5432/valuation
